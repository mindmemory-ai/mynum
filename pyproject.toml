[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "mynum"
version = "0.4.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "mynum_py"
bindings = "pyo3"
python-source = "python"
